<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"             
             Width="800" Height="410">
    <Grid Name="testGrid" Margin="10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
         <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="4*"></ColumnDefinition>
            <ColumnDefinition Width="1*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
	<StackPanel Name="lstPlates" Grid.Column="0"></StackPanel>
	<Grid Name="canvas" Grid.Column="1"></Grid>
	<StackPanel Name="lstAssays" Grid.Column="0"></StackPanel>
				<Label Content="SampleCount" HorizontalAlignment="Left" Margin="186,119,0,0" VerticalAlignment="Top"/>
                <TextBox Name="sampleCnt" HorizontalAlignment="Left" Height="34" Margin="260,119,0,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top" Width="120"/>
                <Button FontFamily="SimHei" FontSize="16" Content="Ok" HorizontalAlignment="Left" Margin="260,198,0,0" VerticalAlignment="Top" Width="120" Height="30"
				xmlns:custom="clr-namespace:Tecan.RUP.DefaultControls.AttachedProperties;assembly=Tecan.RUP.DefaultControls"
                custom:AttachedUIProperties.MethodInformation="ShowSettingForm"/>

    </Grid>
</UserControl>
